// import { STATUS_TYPE } from "./store/matchdays/matchdays.slice";

// let activeMatches = 0; // 2

// const checkMatches = () => {
//   const DATA = [
//     { status: STATUS_TYPE.inProgress },
//     { status: STATUS_TYPE.inProgress },
//   ];

//   const activeCount = DATA.filter(
//     (val) => val.status === STATUS_TYPE.inProgress,
//   ).length; // 1;

//   if (activeCount > activeMatches) {
//     activeMatches = activeCount;
//   }

//   if (activeCount < activeMatches) {
//     const TABLE_RESP = fetch();
//     activeMatches = activeCount;
//     // пихнуть TABLE_RESP в БД
//   }
// };
